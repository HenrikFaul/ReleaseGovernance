# v1.0.4_26040701 — Structured AI Dev Prompt

## Objective
Implement Integrations-module-only improvements for canonical deduplication, category grouping, normalized test execution/reporting, endpoint metadata visibility, and shared cross-project behavior.

## Inputs loaded before transformation
- `CHANGELOG.md`
- `codingLessonsLearnt.md`
- `.governance/codingLessonsLearnt.md`
- `AI_DEVELOPER_PROMPTING_MASTER_MANIFESTO.md`
- `.governance/ui_ux_rules.md`
- `.governance/local_context.md`

## Commands
1. Add/confirm canonical integration identity (`canonicalKey`) and optional distinct instance identity (`instanceKey`).
2. Normalize and deduplicate integration inventory from model, not duplicated UI blocks.
3. Group integrations by stable category enum with ordered section headers.
4. Introduce normalized test result schema:
   - `testMode`, `reachable`, `httpStatusCode`, `normalizedStatus`, `errorKind`, `errorMessage`, `checkedAt`.
5. Implement shared testing service:
   - HTTP: capture status; 2xx = success.
   - Non-HTTP/unconfigured: readable operational error.
6. Provide per-card and test-all actions with non-silent loading/success/failure states.
7. Render endpoint metadata (`endpointName`, `endpointUrl`/`resourceLocation`) with meaningful clickable links.
8. Keep all unrelated flows intact.

## Validation checklist
- Deduplication, grouping, per-item test, bulk test, HTTP status handling, operational error messaging, endpoint metadata links, and shared behavior verified.
