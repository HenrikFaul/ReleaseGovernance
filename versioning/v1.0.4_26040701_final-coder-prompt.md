# v1.0.4_26040701 — Final Coder Prompt

Use `lib/integrations.ts` as the canonical shared Integrations behavior module.

Implement only Integrations scope:
- Extend integration contracts with canonical identity + endpoint metadata + normalized test result model.
- Build API route for single/all integration testing returning per-integration normalized results.
- Update Integrations page to render category sections, canonical card inventory, per-card test, and test-all.
- Ensure loading/success/failure is always visible (no silent failure).
- Keep changelog and lessons append-only.
- Preserve existing non-Integrations actions and UI entry points.
