# Bridge notes

Purpose:
- map legacy tenant and project structures into the new runtime aggregate shape
- reduce direct UI dependency on legacy mock-data structures
- enable gradual migration toward persistent repositories

This bridge is intentionally transitional and exists to reduce regression risk during Phase 1.
